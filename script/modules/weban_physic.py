import pygame

use_drop = False

def use_droper(surface, image, x_object, y_object, x_player, y_player keys_press, key_to_active):
    
    if keys_press[key_to_active]:
        pass
        surface.flip()

    